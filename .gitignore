# Large model binaries — use Git LFS or share separately
models/*.pkl
models/*.pt

# Estado local del encoder: manifiesto de entrenamiento y temporales de guardado
# atómico (describen modelos locales, que de por sí no se versionan)
models/*.meta.json
models/*.tmp

# Token-vector cache (derivado del modelo fastText de 1 GB; regenerable)
models/token_vectors.json

# ETH-80 dataset (205 MB) — download separately
data/

# Imágenes de muestra para la app (derivadas de ETH-80; locales, no se versionan)
app_sample_images/

# ConceptNet corpus (474 MB)
cache/conceptnet-assertions-5.7.0.csv.gz

# Python cache
__pycache__/
*.pyc
*.pyo

# Local Claude settings
.claude/
memory/

# External libraries with their own git repos
hetero/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.synctex.gz
*.toc
*.pdf

# Código legacy/histórico — se conserva local pero no viaja en el paquete final
archive/
notes_historicas/

# Backups locales de modelos (1 GB) - no versionar
models_backup_pre_oficializacion/
models_pre_cost_backup/

# Log del benchmark de costo
cost_benchmark.log
models_v4_official_backup/
